# ダッシュボード改善案 ＋ 分析アプリ改善案（提案）

株式会社オンワードコーポレートデザイン｜生成AI出現モニタリング
作成日：2026-07-28 ／ 対象：`monitoring/src/dashboard.py`（内蔵HTML）＋別途構築する分析アプリ
位置づけ：**改善提案のみ**。承認後に別途実装（本書では実コードは改修しません）。

---

## 0. サマリー

現行ダッシュボードは数値・グラフは揃っているが、「どこを見れば何が分かるか」が読み手に伝わらない。改善の柱は3つ。

1. **過去回との比較**：前回比・初回比を「良化↑／悪化↓」の色と言葉で明示し、画面冒頭に1文サマリーと「この画面の見方」を置く。
2. **Set1×Set2比較**：専用タブを新設し、特異度カーブ（D1→D4）に「崖」を図示＋平易な解釈文＋次アクションまで言語化する。
3. **用語・凡例**：SD／安定性／特異度に一言説明を添え、全チャートに単位・注釈を付ける。

いずれも `dashboard.py`（自己完結HTML＋Chart.js）への追記で実装可能。集計側（analytics.py / reporter.py）の出力は現状のまま流用でき、大きなロジック追加は不要。

---

## 1. 現状データの要点（提案の根拠）

`data/reports/trend.json` と `data/results/results_*.csv` の実測（2026-07-16／07-27）から、ダッシュボードが伝えるべき事実は明確。

| 観測タイミング | 内容 | 全体出現率 | 主なヒット |
|---|---|---|---|
| 07-16 16:12 | Set1・78問・1回 | 1.3%（1/78） | C5指名「どのような会社ですか」1問のみ |
| 07-27 17:04 | Set2・60問・1回 | 0.0% | ヒットなし |
| 07-27 17:05 | Set1+Set2・138問・2回 | 平均2.5%（SD±1.1） | 下記 |

07-27 17:05（2回実行）の中身：

- **ヒットはすべてD1（指名）**。D2/D3/D4は全滅（0%）。→ 崖は明確に **D1→D2 の間**。
- D1ですら平均**20%**（1回目3件／2回目1件）。**指名しても当社が出るのは5回に1回**＝指名時の想起・正確性も弱い。
- 安定してヒットするのは実質 `Raffiria`（Q-C3EC-042／Q-S2-C3-D1-02）と社名系のみ。`Q-C5-A6-B8-053`・`Q-S2-C5-D1-01/02` は2回中1回とブレる。
- Set1平均2.0%／Set2平均3.4%。稼働モデルはClaude単体＝非グラウンディング（＝「学習記憶にあるか」を測定中。検索引用の測定はPerplexity等のキー許可後）。

**ダッシュボードが最優先で伝えるべき一言**：「指名ならたまに出る／業種・実績クエリ（D2）以下では消える。埋めるべきは D1→D2 の壁」。現状はこの結論が画面のどこにも文章で書かれていない。

---

## 2. 内蔵ダッシュボードの改善案

### 2-1. 過去回との比較（「時系列トレンド」タブ）

**課題**：KPIカードに前回比/初回比の数値は出るが、良化か悪化かが直感的でない。何を見るべきかの案内もない。

**改善案（実装可能な粒度）**

1. **1文サマリーの自動生成（画面最上部）**
   `trend.json` の `deltas`・`latest`・`insights` から1文を組み立て、カード群の上にバナー表示。
   例：「最新は全体2.5%（前回比 ▲+2.5pt／初回比 ▲+1.2pt）。ヒットは指名質問（D1）に集中。業種・実績質問（D2）以下は0%のままです。」
   - 良化=緑＋「▲改善」、悪化=赤＋「▼低下」、横ばい=グレー＋「±横ばい」。既存の `.up/.down/.flat` クラスをそのまま流用。

2. **「この画面の見方」注記（折りたたみ）**
   各タブ冒頭に1個。トレンドタブ例：
   「① まず上のサマリーで前回からの増減を確認 → ② 全体推移グラフで山/谷を見る → ③ 事業ドメイン別で伸びた/落ちた事業を特定 → ④『施策への示唆』で次にやることを確認」。

3. **KPIカードの矢印を言葉付きに**
   現行 `▲+2.5pt` に「改善／低下」を併記（例：`▲ +2.5pt 改善`）。数値だけより読み手の解釈が速い。

4. **movers表に「見方」1行**
   表の上に「＝前回から出現率が動いた事業。プラス（緑）は改善、マイナス（赤）は低下」。0%横ばいが並ぶ現状でも意味が伝わる。

### 2-2. Set1×Set2比較（新規タブ「Set比較・崖の分析」）

**課題**：Set1とSet2の出現率は別々のグラフに散っており、「セット1とセット2を比べて何が分かるのか」が組み立てられていない。設計書§7の「二値の“出ない”を勾配の“どこから出るか”に変換する」という肝が画面化されていない。

**改善案：専用タブを1枚追加**（既存タブ構造 `.tabs` に3つ目を足すだけ）。構成は以下4ブロック。

1. **特異度カーブ図（主役）**
   横軸 D1→D2→D3→D4、縦軸 出現率。Set2の `tier_stats` を折れ線＋棒で描く。
   - **Set1（≒D4帯／一般需要）を右端に重ねる**：Set1全体平均を「D4相当の基準線」として点線で重畳。§7-1「Set1（≒D4帯）を接続」を可視化。
   - **崖の位置を図中に明示**：出現率が10%を下回る最初のティア間に赤い縦帯＋「崖」ラベル。現状は D1(20%)→D2(0%) に描画される。`analytics._add_tier_insights` が既に崖ティアを算出しているので、その値を図に渡すだけ。

2. **平易な解釈文（図の直下）**
   `insights` の `tier_curve` テキストを整形して大きめに表示。例：
   「**崖は D1→D2**。指名（D1）では20%出るが、業種・実績を問うD2以下では0%に落ちます。埋めるべきギャップは D2（業種特化・実績）。該当業種の実績ページの権威性・被引用の強化が優先です。」

3. **次アクションの示唆（箇条書き1〜3行）**
   崖の位置に応じて定型文を出し分け（テンプレは `dashboard.py` 内に持てる）。
   - 崖=D1→D2 の場合：「① D2で問うている業種（航空・鉄道・建設・大学病院・映画館 等）の実績ページを、社名・ブランド名と業種名がセットで書かれた形に強化 → ② 事例ページに構造化データ（会社名・実績）を付与 → ③ 再測定でD2が0%→数%に動くかを確認」。

4. **Set1×Set2サマリー表**
   | 指標 | Set1（78問・理想ターゲット） | Set2（60問・診断用） |
   |---|---|---|
   | 全体出現率 | 2.0% | 3.4% |
   | 出現の中心 | 指名1問のみ | D1指名のみ |
   | 読み取り | 一般需要では未出現 | 崖=D1→D2 |
   各セルに役割の一言（Set1＝「出したいが出ない質問」／Set2＝「どこから消えるかを測る質問」）。

### 2-3. 凡例・単位・用語の補足

全チャートに以下を付す（`dashboard.py` のHTMLテンプレ／Chart.jsオプションへの追記）。

- **単位**：縦軸は既に `%` 表記。凡例名を平易化（例：`D1`→`D1 指名`、`set2`→`Set2 診断用60問`。※既に `nameMap` があるので文言拡充のみ）。
- **用語ツールチップ（❓アイコン）**：見出し横に小さな `?` を置きホバーで説明。
  - **SD（標準偏差）**：「同じ条件を複数回実行したときの“ブレ”幅。小さいほど毎回同じ結果＝安定。」
  - **安定性スコア**：「毎回同じ判定（出た/出ない）だったセルの割合。100%＝完全に安定。」
  - **特異度（D1〜D4）**：「質問の“当社の指名度・具体度”。D1=社名指名／D2=業種・実績／D3=非指名の高特異度／D4=一般需要。」
- **空データ時の注記**：既に `tier-empty` 等の空表示があるので文言を活かす（「Set2を実行すると表示されます」）。

### 2-4. 実装方針（dashboard.py で対応できる範囲）

すべて `dashboard.py`（自己完結HTML＋Chart.js CDN）への追記で完結する。**集計側（analytics.py / reporter.py）の改修は原則不要**。理由：必要な値（`deltas`・`tier_stats`・`set_stats`・`insights`・`tier_curve`）は `trend.json`/`timing_*.json` に既に出力済み。

| 改善項目 | 追加先 | 集計側の変更 |
|---|---|---|
| 1文サマリーバナー | `renderTrend()` 冒頭にDOM生成 | 不要（deltas/insights を文章化するだけ） |
| 「この画面の見方」注記 | 各 `section` 冒頭に静的HTML | 不要 |
| KPI矢印に言葉併記 | `kpi()`/`dcell()` の文言 | 不要 |
| Set比較タブ（新規） | `.tabs` に3つ目＋`renderSetCompare()` 追加 | 不要（tier_stats/set_stats を再利用） |
| 特異度カーブに崖の縦帯 | Chart.js の annotation 描画（軽量なら塗り分けでも可） | 不要（崖ティアは insights から取得、または閾値<10%で判定） |
| 用語ツールチップ | HTML＋CSSのみ | 不要 |

補足：崖の縦帯を厳密に描くなら Chart.js の `annotation` プラグイン追加が楽だが、CDN依存を増やしたくなければ「崖ティアの棒を赤系に塗る＋図中テキスト」で代替可能（追加ライブラリなし）。

### 2-5. ワイヤーフレーム（テキスト）

新規「Set比較・崖の分析」タブのレイアウト：

```
┌─ タブ: [時系列トレンド] [タイミング詳細] [Set比較・崖の分析]★新規 ─┐
│                                                                      │
│  ▸ この画面の見方（折りたたみ）                                       │
│    Set2の特異度カーブで「どの具体度から当社が消えるか＝崖」を見ます。 │
│                                                                      │
│  ┌───────────── 特異度カーブ（D1→D4） ─────────────┐              │
│  │ 出現率%                                            │              │
│  │  20 ●D1                                            │              │
│  │      \                                             │              │
│  │   0   ●■■■■■●━━━━━━●━━━━━━●   …Set1平均(点線)  │              │
│  │      D1  [崖] D2    D3    D4                        │              │
│  │           ↑赤い縦帯＋「崖」                         │              │
│  └────────────────────────────────────────────────────┘              │
│                                                                      │
│  【解釈】崖は D1→D2。指名では20%出るが、業種・実績(D2)以下で0%に。   │
│         埋めるべきは D2。該当業種の実績ページ強化が優先。            │
│                                                                      │
│  【次アクション】① D2業種の実績ページを社名×業種で強化 …            │
│                                                                      │
│  ┌── Set1×Set2 サマリー表 ──┐                                       │
│  │ 指標 | Set1 | Set2 | 見方 |                                       │
│  └───────────────────────────┘                                       │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 3. 別途構築する「rowデータ×スクレイピング分析アプリ」の改善案

設計書 `AI_Citation_Monitoring_QuestionSet2_設計_v1.md` §8のプロンプト案を土台に発展させる。

### 3-1. 役割分担（内蔵ダッシュボード vs 別アプリ）

境界を明確にし、機能の重複を避ける。

| | 内蔵ダッシュボード（`dashboard.py`） | 別アプリ（新規） |
|---|---|---|
| 目的 | **日々の定点監視・軽量** | **回答全文の深掘り・突合分析** |
| 入力 | `reports/*.json`（集計済み） | `results/*.csv`（回答全文・生データ）＋Web |
| 見るもの | 出現率・推移・崖・ブレ | 回答本文／競合共起／引用URL／自社・競合サイト突合 |
| 更新頻度 | 実行のたび自動生成 | 分析したいときに随時 |
| ネット接続 | 不要（自己完結） | 必要（スクレイピング） |
| 一言 | 「今どうなっているか」 | 「なぜそうなのか・何を直すか」 |

原則：**集計サマリーと崖の把握は内蔵**、**理由の特定（競合・引用元・コンテンツ突合）は別アプリ**。内蔵側で「崖はD2」と分かったら、別アプリで「D2で誰に負けているか・自社実績ページはあるか」を掘る、という導線。

### 3-2. 別アプリの機能リストと優先度

| 優先 | 機能 | 内容 | 入力 |
|---|---|---|---|
| **P1** | 競合共起分析 | 当社が出ない回答（answer）で代わりに挙がる競合企業名を抽出・ランキング。ドメイン/ティア別に「誰に負けているか」を集計 | `answer`列 |
| **P1** | 多軸クロス集計＋ドリルダウン | ドメイン×ティア×タイプ×ステークホルダー×モデル×run日。セルをクリックで該当回答全文へドリルダウン | 全列 |
| **P2** | 引用元URL分析 | ヒット回答が引用する `urls_found` を集計。効いているページ（例 medical/case）を特定しSet1側へ横展開候補に | `urls_found` |
| **P2** | 自社サイト突合 | `Set2_実績スクレイピング_reference.md` を初期データに、AI回答の言及内容と自社掲載実績を突合。「掲載あるのにAI未言及」＝GEO強化余地を可視化 | reference＋自社サイト |
| **P3** | 競合サイト突合 | AI回答に出た競合サイトを取得し、被引用ギャップを比較 | Web（回答に出た競合に限定） |
| **P3** | ブレ/回答差分ビュー | 同一質問の複数回回答を並べ、ブレる質問の文面差を確認（例 C5-053, S2-C5-D1-01/02） | `answer`×run |

優先度の考え方：まず**なぜ出ないか＝競合共起（P1）**と**根拠に辿れるドリルダウン（P1）**を最優先。次に**何を直すか＝引用元・自社突合（P2）**。**競合サイト突合（P3）**は取得制限があり得るため、AI回答に実際に出た競合URLに限定して後回し。

### 3-3. 入力とデータ連携

- 入力は `monitoring/data/results/results_*.csv`（UTF-8 BOM）。列は現行のまま（`answer` 全文・`entities_found`・`urls_found`・`question_set`・`specificity_tier` を含む）＝**追加のエクスポート実装は不要**。
- 自社実績の初期データは `Set2_実績スクレイピング_reference.md`（業種・掲載顧客・事例URL）を利用。
- 突合キー：`urls_found`（URL単位）と `entities_found`／回答本文のキーワード。「AIが言及 × サイトに掲載」の有無マトリクスを作る。
- 内蔵ダッシュボードとは疎結合（同じCSVを読むだけ）。内蔵側の改修とは独立に着手可能。

### 3-4. 別チャットで使える改訂版プロンプト（コピペ用）

> あなたはデータ分析アプリの開発者です。オンワードコーポレートデザインの「生成AI出現モニタリング」のrowデータを分析する、**自己完結型HTMLダッシュボード**を1枚作成してください。ブラウザ単体で開ける形（外部依存はCDNのChart.jsまで、それ以外はインライン）にしてください。
>
> **入力データ**：`monitoring/data/results/results_*.csv`（UTF-8 BOM、複数ファイル）。主な列：run_date, run_timestamp, question_id, axis_domain, domain_label, axis_type, type_label, axis_stakeholder, stakeholder_label, abm_relevant, model_id, model_name, question, answer（回答全文）, mention_detected（True/False）, mention_position, entities_found, urls_found, context_snippet, question_set（set1/set2）, specificity_tier（D1〜D4／set1は空欄）。
>
> **前提（重要）**：稼働モデルはClaude単体＝**非グラウンディング**。測っているのは「AIの学習記憶に当社があるか」。検索引用の測定はPerplexity等のキー許可後（将来）。現状の実測では出現はほぼ**指名質問（D1）に限定**され、業種・実績質問（D2）以下は0%＝崖はD1→D2。この構造を掘り下げるのが本アプリの目的。
>
> **役割分担**：出現率・推移・特異度カーブ・ブレは内蔵ダッシュボードが担当済み。本アプリは**回答全文の内容分析と突合**に特化する（サマリー集計は重複させず、ドリルダウン起点として最小限に）。
>
> **実装する分析（優先度順）**：
> 1. 【P1】競合共起分析：`mention_detected=False` の回答本文（answer）から、当社の代わりに挙がっている**競合企業名を抽出・ランキング**。ドメイン別・特異度ティア別に「どの土俵で誰に負けているか」を集計。
> 2. 【P1】多軸クロス集計＋ドリルダウン：ドメイン×ティア×質問タイプ×ステークホルダー×モデル×run日の出現率クロス表。セルクリックで該当質問の**回答全文**を展開表示。
> 3. 【P2】引用元URL分析：ヒット回答の `urls_found` を集計し、効いているページを特定。
> 4. 【P2】自社サイト突合：`Set2_実績スクレイピング_reference.md`（業種・掲載顧客・事例URL）を初期データに、AI回答の言及と自社掲載実績を突合し「掲載はあるがAI未言及」＝GEO強化余地を可視化。
> 5. 【P3】競合サイト突合：AI回答に実際に出た競合サイトのみ取得し、被引用ギャップを比較（取得制限に配慮し対象を限定）。
>
> **UI**：フィルタ（ドメイン／特異度ティア／質問セット／モデル／期間）付き。グラフ・表・回答全文ドリルダウンを備える。競合共起ランキングとクロス集計を上部、突合ビューを下部に。
>
> **成果物**：単一HTMLファイル（＋必要なら生成用Pythonスクリプト）。まずCSVの実データ構造を1件読んで確認してから実装すること。

---

## 4. 次アクション（提案）

1. 本提案のうち **2-1（過去回比較の言葉・色・サマリー）と2-2（Set比較タブ）** を優先実装対象として承認可否を判断。
2. 承認後、`dashboard.py` に追記実装（集計側は無改修）。ドライランのダミーデータで表示確認。
3. 別アプリは §3-4 の改訂プロンプトで別チャット着手（P1機能から）。
4. Perplexity等グラウンディング型のキー許可後、Set2を再測定し崖カーブの変化を追う。

---

## 参照した既存ファイル（本フォルダ内）

- `monitoring/AI_Citation_Monitoring_Design_v7.md`（総合仕様書 §6-8）
- `AI_Citation_Monitoring_QuestionSet2_設計_v1.md`（§6変化の分析／§7 Set1×Set2比較／§8 分析アプリ案）
- `設問一覧_Set1_Set2_確認用.md`（Set1=78問／Set2=60問・D1〜D4）
- `Set2_実績スクレイピング_reference.md`（自社実績の一次資料）
- `monitoring/src/dashboard.py`・`analytics.py`・`reporter.py`（現行の画面・集計ロジック）
- `monitoring/data/reports/trend.json`／`monitoring/data/results/results_*.csv`（実測値）

*本書は改善提案であり、実コードの改修は含みません（承認後に別途実装）。*
