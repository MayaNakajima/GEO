# 質問セット2 設計書（確定版 v1.0）
## 生成AI出現モニタリング ― オンワードコーポレートデザイン

作成日：2026-07-27　／　状態：**Set 2（60問）確定**　／　対象：既存78問（Set 1）と並行運用する新規セット
関連ファイル：`monitoring/config/questions_set2.json`（機械可読・60問） ／ `Set2_実績スクレイピング_reference.md`（一次資料）

---

## 1. 目的とねらい

Set 1（78問）は「当社を出したいのに、ほぼ出ない」一般需要・課題起点の理想ターゲット質問群。直近ログでは **78問中1問（1.3%）しかヒットせず、それも唯一の直接指名質問**（C5-A6「オンワードコーポレートデザインはどのような会社ですか？」）だった。非指名カテゴリは全ドメイン0%。

Set 2 は、この「崖」の正体を測るための**診断用セット**。単に「出そうな質問」を並べるのではなく、**指名度・特異度のグラデーション（D1→D4）**を軸に設計し、「どの特異度から出現が消えるか」を可視化する。最上位ティア（D1＝指名）が、そのまま「指名時にAIが当社を正しく説明するか」の**評判・正確性モニタ**を兼ねる（当初の目的1と3を1セットに内包）。

## 2. 設計思想：特異度4ティア

| ティア | 定義 | 主な質問タイプ | ねらい |
|---|---|---|---|
| **D1 指名** | 社名・ブランド名を含む | A6 企業情報 | ほぼ確実に出る。評判・正確性の監視 |
| **D2 業種特化・実績** | 実在の業種・業界＋実績を問う（顧客名は使わず一般化） | A5 事例実績 | 「崖の手前」。ここで出れば大きな示唆 |
| **D3 高特異度・非指名** | 社名なしで当社が“唯一の自然な答え”になる立ち位置 | A1/A3/A7 | 勝てる非指名クエリの探索。GEOの主戦場 |
| **D4 一般需要** | Set 1相当の一般質問 | A1/A2 | 対照群。Set 1と接続する基準線 |

D2・D3が「崖のありか」を映す最重要ゾーンのため、解像度を厚く配分（D1×2・**D2×4・D3×4**・D4×2）。

**構成**：5ドメイン（C1/C3/C4/C5＋**C7**）× 12問（D1:2, D2:4, D3:4, D4:2）＝ **合計60問**。
D2・D3は `Set2_実績スクレイピング_reference.md` で確認した**実在の実績に基づく**が、**具体的な顧客名は使わず業種・業界に一般化**している（例：ANA→「航空会社」、大成建設→「建設会社」、京都府立医大→「大学病院」、TOHOシネマズ→「映画館」、デニーズ→「ファミリーレストラン」）。

**C7「ABM横断・コトデザイン」**（12問・全問 abm_relevant=true）は、御社の戦略的ポジショニング（**モノ＝制服からコト＝顧客ブランド支援へのシフト／クライアントの会社・コーポレートをデザインする／事業横断で1社を深く支援するABM**）を文脈にした設問群。target_site はコーポレートサイト。最も抽象度が高く競合（ブランディング会社・コンサル）と競合する領域のため、当社の“ありたい”ポジションとAI出現の乖離を測るのに最重要。

## 3. 60問一覧

設問の全文は **`設問一覧_Set1_Set2_確認用.md`**（Set1・Set2併記）および機械可読の **`monitoring/config/questions_set2.json`** を参照。
顧客名は業種・業界へ置換済み（残存ゼロを確認）。ティア別内訳：D1指名10／D2業種特化20／D3高特異度20／D4一般10。ドメイン別：C1/C3/C4/C5/C7 各12問。

## 4. 判定ロジックと検出設定

Set 1と同一の判定（①社名 ②ブランド名 ③ドメイン別URL のいずれか出現でヒット）をそのまま使用。Set 2独自の検出追加は不要。

**✅ 修正済み（2026-07-27）**：`config/detection_keywords.json` の `domain_urls` がドメインコードとサブドメインURLでずれていた（C2にuniform、C4にschool、C5にsolutionが誤割当、C1にuniformが未設定）。社名・コーポレートURL検出は効くため致命的ではなかったが、サブドメインURL引用の取りこぼし／誤カウントの原因になるため、下記へ修正した：

```
"C1": ["onward-cd.co.jp", "uniform.onward-cd.co.jp"],
"C3": ["onward-cd.co.jp", "medical.onward-cd.co.jp"],
"C4": ["onward-cd.co.jp", "solution.onward-cd.co.jp"],
"C5": ["onward-cd.co.jp"]
```

## 5. 実装方針（カテゴリ選択を壊さず並行運用）

現行のチェック方法（カテゴリ別・複数カテゴリ・全選択＝`axis_domain` C1〜C7フィルタ）を維持したまま、Set 2を追加する。Set 2はドメインコードをSet 1と共通（C1/C3/C4/C5/C7）にしているため、**カテゴリ選択UIはそのまま流用できる**。

推奨方式は**別ファイル＋セット選択の追加**：

- `config/questions.json`（Set 1・78問）は**一切変更しない**。
- `config/questions_set2.json`（Set 2・60問）を新規追加（確定済み）。
- 実行時に「質問セット」を選ぶUI/引数を1つ足す（例：`--set set1|set2|both`）。ロード対象ファイルを切り替えるだけで、既存のドメインフィルタ・レポート生成はそのまま動く。
- 各質問に付与済みの `question_set`（"set2"）と `specificity_tier`（D1〜D4）フィールドを、結果CSVの出力列に追加すると、後段の比較分析が容易になる。

この方式なら、Set 1のモニタリングは完全に不変のまま、Set 2を独立に（またはbothで）実行できる。実際のコード改修（`runner.py`/`engine.py`/`webapp` のセット選択対応）は、本設計が確定してから着手する。

## 6. 変化の分析

### 6-1. Set 1 の変化分析（継続）
既存の3層レポート（Tier1一回ごと／Tier2ブレ／Tier3トレンド）をそのまま継続。着眼点は「非指名で1件でも出始めるか」。現状ベースライン＝全体1.3%・非指名0%を起点に、施策後の改善を追う。

### 6-2. Set 2 の変化分析（新規）
Set 1と同じ3層に加え、**特異度ティア別の出現率**を主指標にする。

- **出現率カーブ（D1→D4）**：各ティアの出現率を並べ、「崖の位置」を数値化。現状の見立てはD1のみ立ち上がり。
- ティア別の推移（前回比・初回比）で、施策がどのティアの崖を押し下げたか（D2/D3に出現が伸びたか）を追う。
- D2で使った**業種・業界ごとの出現有無**＝「その業種での実績をAIが当社と結びつけているか」の指標。

## 7. Set 1 × Set 2 比較分析の設計（示唆の出し方）

単純比較（Set1=出ない／Set2=出る）では示唆は薄い。**特異度軸を挟むことで、二値の「出ない」を勾配の「どこから出るか」に変換**するのが肝。具体的な見方は4つ。

1. **特異度しきい値の特定**：Set2のD1〜D4カーブと、Set1（≒D4帯）を接続。「当社は特異度◯◯までは出るが、それ未満では消える」という閾値を出す。この閾値と現状コンテンツの差が、埋めるべきGEOギャップの正体。
2. **マッチドペア比較**：同一ドメインでD2⇔D4など特異度だけが違うペアを比べ、特異度の純粋効果を分離（他条件を揃えた対照）。
3. **引用元URL分析**：Set2でヒットした回答が**どのURL・どの実績ページを引用しているか**を集計。効くコンテンツ（例：medical/caseの病院事例、solution/caseのANA事例）が判明したら、それをSet1側の質問に対応するページへ横展開する。
4. **競合共起分析**：当社が出ない所（Set1）で代わりに出ている競合名と、当社が出る所（Set2）の競合密度を比較。「誰に・どの土俵で負けているか」を特定。

> 補足（重要な解釈上の注意）：現状の稼働モデルはClaude単体＝非グラウンディングのため、測っているのは「学習記憶にあるか（パラメトリック認知）」。GEO本来の「検索で引用されるか」を測るには、キー許可後に `sonar-pro`（Perplexity）等のグラウンディング型を1つ追加する（将来タスク）。それまではSet 2の結果を「AIの記憶における当社の想起されやすさ」と読む。

## 8. rowデータ×スクレイピング分析アプリ（別チャット用プロンプト案）

「全rowデータ（回答全文・検出結果）の可視化」＋「デジタルコンテンツのスクレイピング」＋「両者の突合分析」は**実現可能**。別チャットで分析アプリを作る際、以下をそのまま貼れる。

---

**（コピペ用プロンプト）**

> あなたはデータ分析アプリの開発者です。オンワードコーポレートデザインの「生成AI出現モニタリング」のrowデータを分析する自己完結型HTMLダッシュボードを作成してください。
>
> **入力データ**：`monitoring/data/results/results_*.csv`（UTF-8 BOM）。主な列：run_date, question_id, axis_domain, domain_label, axis_type, type_label, axis_stakeholder, abm_relevant, model_id, question, answer（回答全文）, mention_detected, mention_position, entities_found, urls_found, context_snippet。Set 2データには question_set, specificity_tier（D1〜D4）も含む。
>
> **やりたい分析**：
> 1. 出現率の多軸クロス集計（ドメイン別・質問タイプ別・ステークホルダー別・特異度ティア別・モデル別・run日別）と時系列トレンド。
> 2. 特異度ティア（D1→D4）別の出現率カーブと「崖の位置」の可視化。Set1×Set2の比較ビュー。
> 3. 回答全文（answer）の内容分析：当社が出ていない回答で「代わりに挙がっている競合企業名」を抽出・ランキング（競合共起分析）。
> 4. 引用元URL（urls_found）の集計：当社がヒットした回答が引用しているページの傾向。
>
> **スクレイピング連携**：当社サイト（uniform / medical / solution / onward-cd.co.jp）および回答中に競合として挙がったサイトをスクレイピングし、「AI回答で言及されている内容」と「実際にサイトに掲載されている実績・コンテンツ」を突合する。当社の実績は `Set2_実績スクレイピング_reference.md` を初期データとして利用可。競合について、AIが挙げた企業が実際にどんなコンテンツを持つかを取得して、当社との被引用ギャップを可視化する。
>
> **成果物**：フィルタ（ドメイン/ティア/モデル/期間）付き、グラフ・表・回答ドリルダウンを備えた1枚のHTMLダッシュボード。ブラウザ単体で開ける自己完結型。

---

**実現可能性メモ**：
- rowデータ分析・可視化：CSVはローカルにあり、全項目そろっているため確実に可能。
- スクレイピング：当社サイトはサーバーレンダリングで取得容易（本チャットで実証済み）。競合サイトも一般公開ページなら取得可能だが、サイトにより構造差・取得制限があり得るため、対象URLは「AI回答に実際に出た競合」に絞るのが効率的。
- 突合分析：スクレイプ結果とrowデータ（answer/urls_found）をキーワード・URL単位で突き合わせれば、「AIが言及＝サイトに掲載あり/なし」のマトリクスを作れる。

## 9. 次アクション（確定後）

1. ✅ 顧客名を業種・業界へ置換済み（残存ゼロ）。`detection_keywords.json` のURL不整合も修正済み。
2. Set1・Set2の設問を確定（`設問一覧_Set1_Set2_確認用.md` でレビュー）。
3. 確定後、`runner.py`/`engine.py`/`webapp` に「質問セット選択」を実装（Set 1は不変）。
4. Set 2 初回実行（Claude単体）→ 特異度カーブのベースライン取得。
5. キー許可後、グラウンディング型モデルを1つ追加。
6. 別チャットでrowデータ分析アプリを構築（§8プロンプト）。
7. （任意）スクール（C2）をSet2に追加する場合は12問追加＝72問。

---
*Set 2（60問・5ドメイン）は確定（2026-07-27）。Set 1（78問）は設問変更なし。次工程：アプリの質問セット選択実装。*
